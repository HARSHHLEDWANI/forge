# Core

Small, dependency-free foundations used across Forge: version info, the
internal error-exception hierarchy, and the explicit (non-global) logging
facility. Git domain logic (objects, refs, index, ...) lands here in later
phases as it is implemented.
