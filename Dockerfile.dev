FROM gcc:13-bookworm

RUN apt-get update \
    && apt-get install -y --no-install-recommends cmake ninja-build libssl-dev libpq-dev postgresql-client \
    && rm -rf /var/lib/apt/lists/*

WORKDIR /workspace
