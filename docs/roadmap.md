# Roadmap

Foundation → Filesystem → Objects → Trees → Index → Commits/Refs → Checkout → Diff → Merge → CLI UX → Reliability → Server → Clone/Fetch/Push → Auth → PostgreSQL → Web UI → Issues/PRs/Reviews → Workers/Backups → CI → Optimization → Scaling.
