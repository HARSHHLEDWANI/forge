# Learning Order

C++/CMake → OS/filesystems → Git internals → hashing/serialization → data structures/DAGs → index/refs → diff/merge → concurrency/crash consistency → networking → security → PostgreSQL → distributed systems.
