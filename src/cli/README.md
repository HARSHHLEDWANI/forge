# CLI

Command-line entry points. Parses arguments into a command and delegates
to Forge Core / Application Services — it must not implement storage or
Git-domain semantics itself (see `architecture.md`).
