# Unit

Fast, isolated tests for individual components (CLI parsing, logging,
and later hashing/serialization/objects/trees/refs/merge). Run via
`ctest` or the `forge_unit_tests` binary; see `tests/support/` for the
in-repo test framework.
